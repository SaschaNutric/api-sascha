'use strict';

const Detalle_plan_ejercicios 	= require('../collections/detalle_plan_ejercicios');
const Detalle_plan_ejercicio  	= require('../models/detalle_plan_ejercicio');

function getDetalle_plan_ejercicios(req, res, next) {
	Detalle_plan_ejercicios.query(function (qb) {
   		qb.where('detalle_plan_ejercicio.estatus', '=', 1);
	})
	.fetch({ columns: ['id_detalle_plan_ejercicio','id_plan_ejercicio','id_ejercicio'] })
	.then(function(data) {
		if (!data)
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'No hay dato registrados' } 
			});

		return res.status(200).json({
			error: false,
			data: data
		});
	})
	.catch(function (err) {
     	return res.status(500).json({
			error: true,
			data: { mensaje: err.message }
		});
    });
}

function saveDetalle_plan_ejercicio(req, res, next){
	console.log(JSON.stringify(req.body));

	Detalle_plan_ejercicio.forge({ id_plan_ejercicio:req.body.id_plan_ejercicio ,id_ejercicio:req.body.id_ejercicio  })
	.save()
	.then(function(data){
		res.status(200).json({
			error: false,
			data: data
		});
	})
	.catch(function (err) {
		res.status(500)
		.json({
			error: true,
			data: {message: err.message}
		});
	});
}

function getDetalle_plan_ejercicioById(req, res, next) {
	const id = Number.parseInt(req.params.id);
	if (!id || id == 'NaN') 
		return res.status(400).json({ 
			error: true, 
			data: { mensaje: 'Solicitud incorrecta' } 
		});

	Detalle_plan_ejercicio.forge({ id_detalle_plan_ejercicio: id })
	.fetch()
	.then(function(data) {
		if(!data) 
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'dato no encontrado' } 
			});
		return res.status(200).json({ 
			error : false, 
			data : data 
		});
	})
	.catch(function(err){
		return res.status(500).json({ 
			error: false, 
			data: { mensaje: err.message } 
		})
	});
}

function updateDetalle_plan_ejercicio(req, res, next) {
	const id = Number.parseInt(req.params.id);
	if (!id || id == 'NaN') {
		return res.status(400).json({ 
			error: true, 
			data: { mensaje: 'Solicitud incorrecta' } 
		});
	}

	Detalle_plan_ejercicio.forge({ id_detalle_plan_ejercicio: id })
	.fetch()
	.then(function(data){
		if(!data) 
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'Solicitud no encontrada' } 
			});
		data.save({ id_plan_ejercicio:req.body.id_plan_ejercicio || data.get('id_plan_ejercicio'),id_ejercicio:req.body.id_ejercicio || data.get('id_ejercicio') })
		.then(function() {
			return res.status(200).json({ 
				error: false, 
				data: data
			});
		})
		.catch(function(err) {
			return res.status(500).json({ 
				error : true, 
				data : { mensaje : err.message } 
			});
		})
	})
	.catch(function(err) {
		return res.status(500).json({ 
			error : true, 
			data : { mensaje : err.message } 
		});
	})
}

function deleteDetalle_plan_ejercicio(req, res, next) {
	const id = Number.parseInt(req.params.id);
	if (!id || id == 'NaN') {
		return res.status(400).json({ 
			error: true, 
			data: { mensaje: 'Solicitud incorrecta' } 
		});
	}
	Detalle_plan_ejercicio.forge({ id_detalle_plan_ejercicio: id })
	.fetch()
	.then(function(data){
		if(!data) 
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'Solicitud no encontrad0' } 
			});

		data.save({ estatus:  0 })
		.then(function() {
			return res.status(200).json({ 
				error: false,
				data: { mensaje: 'Registro eliminado' } 
			});
		})
		.catch(function(err) {
			return res.status(500).json({ 
				error: true, 
				data: { mensaje: err.message} 
			});
		})
	})
	.catch(function(err){
		return res.status(500).json({ 
			error: true, 
			data: { mensaje: err.message } 
		});
	})
}

module.exports = {
	getDetalle_plan_ejercicios,
	saveDetalle_plan_ejercicio,
	getDetalle_plan_ejercicioById,
	updateDetalle_plan_ejercicio,
	deleteDetalle_plan_ejercicio
}
