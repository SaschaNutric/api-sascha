'use strict';

const Regimen_suplementos 	= require('../collections/regimen_suplementos');
const Regimen_suplemento  	= require('../models/regimen_suplemento');

function getRegimen_suplementos(req, res, next) {
	Regimen_suplementos.query(function (qb) {
   		qb.where('regimen_suplemento.estatus', '=', 1);
	})
	.fetch({ columns: ['id_plan_suplemento','id_cliente','id_frecuencia','cantidad'] })
	.then(function(data) {
		if (!data)
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'No hay dato registrados' } 
			});

		return res.status(200).json({
			error: false,
			data: data
		});
	})
	.catch(function (err) {
     	return res.status(500).json({
			error: true,
			data: { mensaje: err.message }
		});
    });
}

function saveRegimen_suplemento(req, res, next){
	console.log(JSON.stringify(req.body));

	Regimen_suplemento.forge({ id_plan_suplemento:req.body.id_plan_suplemento ,id_cliente:req.body.id_cliente ,id_frecuencia:req.body.id_frecuencia ,cantidad:req.body.cantidad  })
	.save()
	.then(function(data){
		res.status(200).json({
			error: false,
			data: data
		});
	})
	.catch(function (err) {
		res.status(500)
		.json({
			error: true,
			data: {message: err.message}
		});
	});
}

function getRegimen_suplementoById(req, res, next) {
	const id = Number.parseInt(req.params.id);
	if (!id || id == 'NaN') 
		return res.status(400).json({ 
			error: true, 
			data: { mensaje: 'Solicitud incorrecta' } 
		});

	Regimen_suplemento.forge({ id_regimen_suplemento })
	.fetch()
	.then(function(data) {
		if(!data) 
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'dato no encontrado' } 
			});
		return res.status(200).json({ 
			error : false, 
			data : data 
		});
	})
	.catch(function(err){
		return res.status(500).json({ 
			error: false, 
			data: { mensaje: err.message } 
		})
	});
}

function updateRegimen_suplemento(req, res, next) {
	const id = Number.parseInt(req.params.id);
	if (!id || id == 'NaN') {
		return res.status(400).json({ 
			error: true, 
			data: { mensaje: 'Solicitud incorrecta' } 
		});
	}

	Regimen_suplemento.forge({ id_regimen_suplemento })
	.fetch()
	.then(function(data){
		if(!data) 
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'Solicitud no encontrada' } 
			});
		data.save({ id_plan_suplemento:req.body.id_plan_suplemento || data.get('id_plan_suplemento'),id_cliente:req.body.id_cliente || data.get('id_cliente'),id_frecuencia:req.body.id_frecuencia || data.get('id_frecuencia'),cantidad:req.body.cantidad || data.get('cantidad') })
		.then(function() {
			return res.status(200).json({ 
				error: false, 
				data: data
			});
		})
		.catch(function(err) {
			return res.status(500).json({ 
				error : true, 
				data : { mensaje : err.message } 
			});
		})
	})
	.catch(function(err) {
		return res.status(500).json({ 
			error : true, 
			data : { mensaje : err.message } 
		});
	})
}

function deleteRegimen_suplemento(req, res, next) {
	const id = Number.parseInt(req.params.id);
	if (!id || id == 'NaN') {
		return res.status(400).json({ 
			error: true, 
			data: { mensaje: 'Solicitud incorrecta' } 
		});
	}
	Regimen_suplemento.forge({ id_regimen_suplemento })
	.fetch()
	.then(function(data){
		if(!data) 
			return res.status(404).json({ 
				error: true, 
				data: { mensaje: 'Solicitud no encontrad0' } 
			});

		data.save({ estatus:  0 })
		.then(function() {
			return res.status(200).json({ 
				error: false,
				data: { mensaje: 'Registro eliminado' } 
			});
		})
		.catch(function(err) {
			return res.status(500).json({ 
				error: true, 
				data: { mensaje: err.message} 
			});
		})
	})
	.catch(function(err){
		return res.status(500).json({ 
			error: true, 
			data: { mensaje: err.message } 
		});
	})
}

module.exports = {
	getRegimen_suplementos,
	saveRegimen_suplemento,
	getRegimen_suplementoById,
	updateRegimen_suplemento,
	deleteRegimen_suplemento
}
